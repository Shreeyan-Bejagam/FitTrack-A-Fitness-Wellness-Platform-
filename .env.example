# Local development (Vite loads .env.development automatically)
VITE_API_URL=http://localhost:5001

# Production (Netlify / Vercel): set VITE_API_URL in the host dashboard
# to your deployed API origin with NO trailing slash, e.g.:
# VITE_API_URL=https://fittrack-api.onrender.com
